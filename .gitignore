node_modules/
# `.env` sozinho casa exatamente com `.env` e mais nada — `.env.local`,
# `.env.bak`, `.env.production` ficavam desprotegidos num repo PUBLICO.
.env*
!.env.example
.claude/
.serena/
design_handoff_v2/
.playwright-mcp/
.DS_Store
supabase/.temp/

# Operational docs and generated reports — this repo is PUBLIC.
# These carry infrastructure maps and customer conversation data.
reports/
HANDOFF-MIGRACAO.md

# Scratch do subagent-driven-development (ledger, briefs, review packages)
.superpowers/

# Link local com a Vercel (ids de projeto/org)
.vercel/
.vercel
